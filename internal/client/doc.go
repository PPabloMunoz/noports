// Package client helps CLI commands talk to the daemon. It ensures the CA and daemon are running, dials the control socket, encodes requests, decodes responses, and formats human-readable output.
package client
